public class SimpleAlgorithms {
    private int[] data;

    public SimpleAlgorithms(int[] data) {
        this.data = data;
    }

    // helper function that prints the data array to a new line
    // should use for each loop
    // i.e. formatted like: 5, 3, 2, 6, 1
    private void printData() {
    }

    // returns the minimum value of data
    public int findMin() {
        return 0;
    }

    // returns the maximum value of data
    public int findMax() {
        return 0;
    }

    // swaps data[i] with data[j]
    // calls the printData helper function
    public void swap(int i, int j) {
    }

    // returns the average value of data
    public double avg() {
        return 0.0;
    }

    // sorts data
    // calls the printData helper function
    public void sort() {

    }
}
